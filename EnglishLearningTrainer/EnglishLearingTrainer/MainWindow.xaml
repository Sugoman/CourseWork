<Window x:Class="LearningTrainer.MainWindow"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"        
        xmlns:views="clr-namespace:LearningTrainer.Views"
        xmlns:viewmodels="clr-namespace:LearningTrainer.ViewModels"
        Title="English Learning Trainer" 
        Height="720" Width="1280"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="Transparent"
        Name="AppWindow">
    
    <Window.DataContext>
        <viewmodels:MainViewModel/>
    </Window.DataContext>

    <!-- Основной контейнер с тенью и скругленными углами -->
    <Border Background="#F0F2F5" CornerRadius="10" BorderThickness="1" BorderBrush="#DCDCDC">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="15" Color="Black" Opacity="0.2"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Height="30" Background="Transparent" MouseDown="TitleBar_MouseDown">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="_" Width="30" Click="MinimizeButton_Click" Style="{StaticResource WindowControlButton}"/>
                    <Button Content="☐" Width="30" Click="MaximizeButton_Click" Style="{StaticResource WindowControlButton}"/>
                    <Button Content="✕" Width="30" Click="CloseButton_Click" Style="{StaticResource WindowControlButton}"/>
                </StackPanel>
            </Border>

            <ContentControl Grid.Row="1" Content="{Binding CurrentView}">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type viewmodels:LoginViewModel}">
                        <views:LoginView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type viewmodels:ShellViewModel}">
                        <views:ShellView/>
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>
        </Grid>
    </Border>
</Window>
