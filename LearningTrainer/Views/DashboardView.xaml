<UserControl x:Class="LearningTrainer.Views.DashboardView"
             x:Name="DashboardRoot"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:LearningTrainer.Converters"
             xmlns:local="clr-namespace:LearningTrainer.Views"
             xmlns:system="clr-namespace:System;assembly=mscorlib"             
             xmlns:svg="clr-namespace:SharpVectors.Converters;assembly=SharpVectors.Converters.Wpf"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:lvcSkia="clr-namespace:LiveChartsCore.SkiaSharpView.Painting;assembly=LiveChartsCore.SkiaSharpView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConverter" />
        <converters:PercentToFixedWidthConverter x:Key="PercentToFixedWidthConverter" MaxWidth="400"/>
    </UserControl.Resources>

    <!-- ОСНОВНОЙ МАКЕТ с наложением уведомлений -->
    <Grid>
        <!-- Основной контент -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ===================================== -->
            <!--        Row 0: НАВИГАЦИОННАЯ ПАНЕЛЬ    -->
            <!-- ===================================== -->
            <Border Grid.Row="0" 
                    Background="{DynamicResource SecondaryMainBackgroundBrush}" 
                    BorderBrush="{DynamicResource BorderBrush}" 
                    BorderThickness="0,0,0,1">
                <Grid Margin="20,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">

                            <RadioButton x:Name="HomeRadio" 
                     Content="{DynamicResource Loc.Dashboard.HomeButton}" 
                     GroupName="MainNav" 
                     Style="{StaticResource NavRadioButtonStyle}" Cursor="Hand"/>

                            <RadioButton x:Name="RulesRadio" 
                     Content="{DynamicResource Loc.Dashboard.RuleVocab}" 
                     GroupName="MainNav" 
                     Style="{StaticResource NavRadioButtonStyle}" 
                     IsChecked="True"/>

                        </StackPanel>

                        <Border BorderBrush="{DynamicResource SeparatorBrush}" BorderThickness="1,0,0,0" Height="24" Margin="15,0"/>

                        <Button x:Name="AddButton" 
                                Content="{DynamicResource Loc.Dashboard.AddButton}" 
                                Style="{StaticResource NavButtonStyle}" 
                                Click="AddButton_Click"
                        Visibility="{Binding CanManage, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Button.ContextMenu>
                                <ContextMenu Background="{DynamicResource SecondaryMainBackgroundBrush}" >
                                    <MenuItem Header="{DynamicResource Loc.Dashboard.AddRule}" 
                                              Foreground="{DynamicResource PrimaryTextBrush}" 
                                              Command="{Binding CreateRuleCommand}"/>

                                    <MenuItem Header="{DynamicResource Loc.Dashboard.AddVocabulary}" 
                                              Foreground="{DynamicResource PrimaryTextBrush}" 
                                              Command="{Binding CreateDictionaryCommand}"/>
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>

                        <Button Content="{DynamicResource Loc.Button.ImportButton}" 
                        Style="{StaticResource NavButtonStyle}"
                        Command="{Binding ImportDictionaryCommand}"
                        Visibility="{Binding CanManage, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                        <Button Style="{StaticResource NavButtonStyle}" Command="{Binding ExportAllCommand}" Margin="5,0,0,0"
                                Visibility="{Binding CanManage, Converter={StaticResource BooleanToVisibilityConverter}}"
                                ToolTip="Экспортировать все словари">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📦" FontSize="14" VerticalAlignment="Center"/>
                                <TextBlock Text="Экспорт" Margin="4,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource NavButtonStyle}" Command="{Binding ImportCsvCommand}" Margin="5,0,0,0"
                                Visibility="{Binding CanManage, Converter={StaticResource BooleanToVisibilityConverter}}"
                                ToolTip="Импорт CSV файла">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📄" FontSize="14" VerticalAlignment="Center"/>
                                <TextBlock Text="CSV" Margin="4,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource NavButtonStyle}" Command="{Binding OpenMarketplaceCommand}" Margin="10,0,0,0">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🛒" FontSize="16" VerticalAlignment="Center"/>
                                        <TextBlock Text="Маркетплейс" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Style="{StaticResource NavButtonStyle}" Command="{Binding OpenStatisticsCommand}" Margin="10,0,0,0">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📊" FontSize="16" VerticalAlignment="Center"/>
                                        <TextBlock Text="Статистика" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Content="Settings" Style="{StaticResource NavButtonStyle}" Command="{Binding OpenSettingsCommand}" Margin="20,0,0,0">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Rectangle Height="20" Width="20" Fill="{DynamicResource PrimaryTextBrush}">
                                            <Rectangle.OpacityMask>
                                                <VisualBrush Stretch="Uniform">
                                                    <VisualBrush.Visual>
                                                        <svg:SvgViewbox Source="Sources/settings.svg" />
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Rectangle.OpacityMask>
                                        </Rectangle>




                                        <TextBlock Text="{DynamicResource Loc.Button.SettingsButton}" 
                                                   Margin="5 0 0 0"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                        <ComboBox ItemsSource="{Binding DisplaySortOptions}"
                         ItemContainerStyle="{StaticResource ComboBoxItemContainerStyle}"
                         SelectedValue="{Binding SelectedSortKey}"
                         Foreground="{DynamicResource PrimaryTextBrush}"
                         Style="{StaticResource SortComboBoxStyle}"
                         DisplayMemberPath="DisplayName"
                         SelectedValuePath="Key"
                         MinWidth="210"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">

                        <Grid>
                            <Border Background="{DynamicResource SecondaryMainBackgroundBrush}" 
                    BorderBrush="{DynamicResource SeparatorBrush}"
                    BorderThickness="1"
                    CornerRadius="8" 
                    Width="300" Height="35"/>

                            <StackPanel Orientation="Horizontal">
                                <Rectangle Fill="{DynamicResource PrimaryTextBrush}"
                           Height="20" Width="20"
                           Stretch="Uniform" 
                           Margin="12,0,5,0" 
                           VerticalAlignment="Center">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Uniform">
                                            <VisualBrush.Visual>
                                                <svg:SvgViewbox Source="Sources/search.svg"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Rectangle.OpacityMask>
                                </Rectangle>

                                <TextBox Background="Transparent" BorderThickness="0" Padding="8" Foreground="{DynamicResource PrimaryTextBrush}"
                     VerticalContentAlignment="Center" Width="250" FontSize="14"
                     x:Name="SearchTextBox"
                     Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>

                            <TextBlock Text="{DynamicResource Loc.Dashboard.SearchPlaceholder}" 
                       Foreground="{DynamicResource PrimaryTextBrush}" 
                       VerticalAlignment="Center" 
                       Margin="50,0,0,0" IsHitTestVisible="False"
                       Visibility="{Binding Text, ElementName=SearchTextBox, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}"/>
                        </Grid>

                        <Button Command="{Binding RefreshCommand}" 
                Style="{StaticResource NavButtonStyle}" 
                Margin="10,0,0,0"
                ToolTip="Refresh Data"
                Width="35" Height="35"
                Padding="0">
                            <Button.Content>
                                <Rectangle Width="18" Height="18" Fill="{DynamicResource PrimaryTextBrush}">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Uniform">
                                            <VisualBrush.Visual>
                                                <Path Data="M17.65 6.35C16.2 4.9 14.21 4 12 4c-4.42 0-7.99 3.58-7.99 8s3.57 8 7.99 8c3.73 0 6.84-2.55 7.73-6h-2.08c-.82 2.33-3.04 4-5.65 4-3.31 0-6-2.69-6-6s2.69-6 6-6c1.66 0 3.14.69 4.22 1.78L13 11h7V4l-2.35 2.35z" Fill="Black"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                            </Button.Content>
                        </Button>

                    </StackPanel>
                </Grid>
            </Border>

            <!-- ===================================== -->
            <!--        Row 1: КОНТЕНТНАЯ ЗОНА         -->
            <!-- ===================================== -->
            <Grid Grid.Row="1">

                <Grid Margin="30" Visibility="{Binding IsChecked, ElementName=HomeRadio, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ScrollViewer>
                        <StackPanel>
                            <TextBlock Text="Your Progress" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,20"/>

                            <!-- Streak banner -->
                            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Padding="16,12" Margin="0,0,0,15"
                                    BorderBrush="{DynamicResource PrimaryAccentBrush}" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="🔥" FontSize="32" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Stats.CurrentStreak}" FontSize="28" FontWeight="Bold"
                                                       Foreground="{DynamicResource PrimaryAccentBrush}"/>
                                            <TextBlock Text=" дн. подряд" FontSize="16" VerticalAlignment="Bottom" Margin="0,0,0,4"
                                                       Foreground="{DynamicResource PrimaryTextBrush}"/>
                                        </StackPanel>
                                        <TextBlock Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12">
                                            <TextBlock.Text>
                                                <Binding Path="Stats.BestStreak" StringFormat="Лучший результат: {0} дн."/>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </StackPanel>
                                    <!-- Streak progress dots (7 days) -->
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding Stats.ActivityLast7Days}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Ellipse Width="10" Height="10" Margin="2,0" VerticalAlignment="Center">
                                                        <Ellipse.Style>
                                                            <Style TargetType="Ellipse">
                                                                <Setter Property="Fill" Value="{DynamicResource BorderBrush}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Reviewed, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=inverse}" Value="Collapsed">
                                                                        <Setter Property="Fill" Value="{DynamicResource SuccessBrush}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Ellipse.Style>
                                                    </Ellipse>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- XP Level bar (§18.5a LEARNING_IMPROVEMENTS) -->
                            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Padding="16,12" Margin="0,0,0,15"
                                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                    Visibility="{Binding HasXp, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Level badge -->
                                    <Border Grid.Column="0" Background="{DynamicResource PrimaryAccentBrush}" CornerRadius="20"
                                            Width="40" Height="40" Margin="0,0,12,0">
                                        <TextBlock Text="{Binding XpLevel}" FontSize="18" FontWeight="Bold" Foreground="White"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>

                                    <!-- Progress bar -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4">
                                            <Run Text="Уровень"/>
                                            <Run Text="{Binding XpLevel, Mode=OneWay}"/>
                                        </TextBlock>
                                        <Border Background="{DynamicResource BorderBrush}" CornerRadius="4" Height="8">
                                            <Border Background="{DynamicResource PrimaryAccentBrush}" CornerRadius="4" HorizontalAlignment="Left"
                                                    Width="{Binding XpProgressPercent, Converter={StaticResource PercentToFixedWidthConverter}}"/>
                                        </Border>
                                    </StackPanel>

                                    <!-- XP text -->
                                    <TextBlock Grid.Column="2" Text="{Binding XpProgressText}" FontSize="14" FontWeight="SemiBold"
                                               Foreground="{DynamicResource PrimaryAccentBrush}" VerticalAlignment="Center" Margin="12,0,0,0"/>
                                </Grid>
                            </Border>

                            <!-- Daily Plan (§18.2 LEARNING_IMPROVEMENTS) -->
                            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,15"
                                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                    Visibility="{Binding HasDailyPlan, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="📋 Дневной план" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                            <TextBlock Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,2,0,0">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}{0} к повторению · {1} новых · {2} сложных">
                                                        <Binding Path="DailyPlan.Stats.TotalReviewCount"/>
                                                        <Binding Path="DailyPlan.Stats.TotalNewCount"/>
                                                        <Binding Path="DailyPlan.Stats.TotalDifficultCount"/>
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                        </StackPanel>
                                        <Button Grid.Column="1" Command="{Binding StartSmartTrainingCommand}"
                                                Style="{StaticResource NavButtonStyle}" Padding="16,8">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🚀" FontSize="16" VerticalAlignment="Center"/>
                                                <TextBlock Text="Начать" Margin="6,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                            </StackPanel>
                                        </Button>
                                    </Grid>

                                    <!-- Daily Goal progress bar -->
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="🎯 Дневная цель" FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                                            <Border Background="{DynamicResource BorderBrush}" CornerRadius="4" Height="8">
                                                <Border Background="{DynamicResource PrimaryAccentBrush}" CornerRadius="4" HorizontalAlignment="Left"
                                                        Width="{Binding DailyGoalProgress, Converter={StaticResource PercentToFixedWidthConverter}}"/>
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{Binding DailyGoalText}" FontSize="14" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource PrimaryAccentBrush}" VerticalAlignment="Bottom" Margin="12,0,0,0"/>
                                    </Grid>

                                    <!-- Category buttons -->
                                    <UniformGrid Columns="3" Margin="0,4,0,0">
                                        <Button Command="{Binding StartReviewCommand}" Style="{StaticResource NavButtonStyle}" Margin="0,0,4,0" Padding="10,8">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🔄" FontSize="14" VerticalAlignment="Center"/>
                                                <TextBlock VerticalAlignment="Center" Margin="4,0,0,0">
                                                    <TextBlock.Text>
                                                        <Binding Path="DailyPlan.Stats.TotalReviewCount" StringFormat="Повторить ({0})"/>
                                                    </TextBlock.Text>
                                                </TextBlock>
                                            </StackPanel>
                                        </Button>
                                        <Button Command="{Binding StartNewWordsCommand}" Style="{StaticResource NavButtonStyle}" Margin="4,0,4,0" Padding="10,8">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="✨" FontSize="14" VerticalAlignment="Center"/>
                                                <TextBlock VerticalAlignment="Center" Margin="4,0,0,0">
                                                    <TextBlock.Text>
                                                        <Binding Path="DailyPlan.Stats.TotalNewCount" StringFormat="Новые ({0})"/>
                                                    </TextBlock.Text>
                                                </TextBlock>
                                            </StackPanel>
                                        </Button>
                                        <Button Command="{Binding StartDifficultCommand}" Style="{StaticResource NavButtonStyle}" Margin="4,0,0,0" Padding="10,8">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="💪" FontSize="14" VerticalAlignment="Center"/>
                                                <TextBlock VerticalAlignment="Center" Margin="4,0,0,0">
                                                    <TextBlock.Text>
                                                        <Binding Path="DailyPlan.Stats.TotalDifficultCount" StringFormat="Сложные ({0})"/>
                                                    </TextBlock.Text>
                                                </TextBlock>
                                            </StackPanel>
                                        </Button>
                                    </UniformGrid>

                                    <!-- Leech warning -->
                                    <Border Visibility="{Binding HasLeechWords, Converter={StaticResource BooleanToVisibilityConverter}}"
                                            Background="#FFF3CD" CornerRadius="8" Padding="10,8" Margin="0,10,0,0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                <TextBlock Text="🐛" FontSize="16" VerticalAlignment="Center"/>
                                                <TextBlock VerticalAlignment="Center" Margin="6,0,0,0" Foreground="#856404" FontSize="12">
                                                    <Run Text="{Binding LeechCountText, Mode=OneWay}"/>
                                                    <Run Text="слов заморожено — они вызывают постоянные трудности"/>
                                                </TextBlock>
                                            </StackPanel>
                                            <Button Grid.Column="1" Content="Управление" Command="{Binding OpenLeechManagerCommand}"
                                                    Background="#856404" Foreground="White" BorderThickness="0" Cursor="Hand"
                                                    Padding="12,4" FontSize="11">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </StackPanel>
                            </Border>

                            <UniformGrid Columns="2" Rows="2" >

                                <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Padding="20" Margin="0,0,15,15">
                                    <StackPanel>
                                        <TextBlock Text="Total Words" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="14"/>
                                        <TextBlock Text="{Binding Stats.TotalWords}" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="32" FontWeight="Bold" Margin="0,10,0,0"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                                    <StackPanel>
                                        <TextBlock Text="Learned Words" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="14"/>
                                        <TextBlock Text="{Binding Stats.LearnedWords}" Foreground="{DynamicResource SuccessBrush}" FontSize="32" FontWeight="Bold" Margin="0,10,0,0"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Padding="20" Margin="0,0,15,0">
                                    <StackPanel>
                                        <TextBlock Text="Dictionaries" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="14"/>
                                        <TextBlock Text="{Binding Stats.TotalDictionaries}" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="32" FontWeight="Bold" Margin="0,10,0,0"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Padding="20">
                                    <StackPanel>
                                        <TextBlock Text="Avg. Success Rate" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="14"/>
                                        <TextBlock Text="{Binding Stats.AverageSuccessRate, StringFormat={}{0:P0}}" Foreground="{DynamicResource PrimaryAccentBrush}" FontSize="32" FontWeight="Bold" Margin="0,10,0,0"/>
                                    </StackPanel>
                                </Border>

                            </UniformGrid>

                            <StackPanel Margin="0,20,0,0">
                                <TextBlock Text="Активность за 7 дней" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,8"/>
                                <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Padding="10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                                    <lvc:CartesianChart Height="240"
                                                       Series="{Binding ActivitySeries}"
                                                       XAxes="{Binding ActivityXAxis}"
                                                       YAxes="{Binding ActivityYAxis}"
                                                       LegendPosition="Right"
                                                       LegendTextPaint="{Binding LegendTextPaint}" />
                                </Border>
                            </StackPanel>

                            <StackPanel Margin="0,20,0,0">
                                <TextBlock Text="Распределение уровней знаний" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,8"/>
                                <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Padding="10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                                    <lvc:CartesianChart Height="240"
                                                       Series="{Binding KnowledgeSeries}"
                                                       XAxes="{Binding KnowledgeXAxis}"
                                                       YAxes="{Binding KnowledgeYAxis}"
                                                       LegendPosition="Right"
                                                       LegendTextPaint="{Binding LegendTextPaint}" />
                                </Border>
                            </StackPanel>

                            <!-- Leech Manager Panel (§18.3a) -->
                            <Border Visibility="{Binding ShowLeechPanel, Converter={StaticResource BooleanToVisibilityConverter}}"
                                    Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Padding="20" Margin="0,20,0,0"
                                    BorderBrush="#E0A800" BorderThickness="1">
                                <StackPanel>
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="🐛 Замороженные слова" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                            <TextBlock Text="Эти слова вызывают постоянные трудности. Нажмите «Разморозить», чтобы вернуть в очередь."
                                                       FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                        </StackPanel>
                                        <Button Grid.Column="1" Content="✕" Command="{Binding CloseLeechPanelCommand}"
                                                Background="Transparent" BorderThickness="0" FontSize="18" Cursor="Hand"
                                                Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Top"/>
                                    </Grid>

                                    <ItemsControl ItemsSource="{Binding LeechWords}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{DynamicResource SecondaryMainBackgroundBrush}" CornerRadius="8"
                                                        Padding="12,8" Margin="0,4"
                                                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="{Binding OriginalWord}" FontSize="14" FontWeight="SemiBold"
                                                                           Foreground="{DynamicResource PrimaryTextBrush}"/>
                                                                <TextBlock Text=" → " Foreground="{DynamicResource SecondaryTextBrush}" FontSize="14"/>
                                                                <TextBlock Text="{Binding Translation}" FontSize="14"
                                                                           Foreground="{DynamicResource SecondaryTextBrush}"/>
                                                            </StackPanel>
                                                            <TextBlock FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Opacity="0.7">
                                                                <Run Text="Сбросов:"/>
                                                                <Run Text="{Binding LapseCount, Mode=OneWay}"/>
                                                                <Run Text="· Словарь:"/>
                                                                <Run Text="{Binding DictionaryName, Mode=OneWay}"/>
                                                            </TextBlock>
                                                        </StackPanel>
                                                        <TextBlock Grid.Column="1" Text="{Binding Transcription}" FontSize="12"
                                                                   Foreground="{DynamicResource PrimaryAccentBrush}" VerticalAlignment="Center"
                                                                   Margin="8,0">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Transcription}" Value="{x:Null}">
                                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Transcription}" Value="">
                                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                        <Button Grid.Column="2" Content="Разморозить"
                                                                Command="{Binding DataContext.UnsuspendWordCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                Background="#28a745" Foreground="White" BorderThickness="0" Cursor="Hand"
                                                                Padding="10,4" FontSize="11" VerticalAlignment="Center">
                                                            <Button.Template>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border Background="{TemplateBinding Background}" CornerRadius="4"
                                                                            Padding="{TemplateBinding Padding}">
                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Button.Template>
                                                        </Button>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                        </StackPanel>
                    </ScrollViewer>
                </Grid>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                      Visibility="{Binding IsChecked, ElementName=RulesRadio, Converter={StaticResource BooleanToVisibilityConverter}}">

                    <StackPanel Margin="30">
                        <StackPanel.Resources>
                            <Style x:Key="RuleCardStyle" TargetType="Border">
                                <Setter Property="MinWidth" Value="200"/>
                                <Setter Property="MinHeight" Value="100"/>
                                <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
                                <Setter Property="CornerRadius" Value="12"/>
                                <Setter Property="BorderThickness" Value="3"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                <Setter Property="Margin" Value="8"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsFeatured}" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource PrimaryAccentBrush}"/>
                                        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryAccentBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>

                            <Style x:Key="VocabCardStyle" TargetType="Border">
                                <Setter Property="Width" Value="300"/>
                                <Setter Property="Height" Value="200"/>
                                <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
                                <Setter Property="CornerRadius" Value="12"/>
                                <Setter Property="BorderThickness" Value="5"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                <Setter Property="Margin" Value="8"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsFeatured}" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource PrimaryAccentBrush}"/>
                                        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryAccentBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>

                            <Style x:Key="PlayButtonStyle" TargetType="Button">
                                <Setter Property="Width" Value="44"/>
                                <Setter Property="Height" Value="44"/>
                                <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryAccentBrush}"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="22">
                                                <Rectangle Width="30" Height="30" Fill="{DynamicResource SecondaryTextBrush}">
                                                    <Rectangle.OpacityMask>
                                                        <VisualBrush Stretch="Uniform">
                                                            <VisualBrush.Visual>
                                                                <svg:SvgViewbox Source="Sources/start.svg"/>
                                                            </VisualBrush.Visual>
                                                        </VisualBrush>
                                                    </Rectangle.OpacityMask>
                                                </Rectangle>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                                    </Trigger>
                                    <DataTrigger Binding="{Binding IsFeatured}" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource PrimaryAccentBrush}"/>
                                        <Setter Property="Foreground" Value="{DynamicResource AccentForegroundBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Resources>


                        <TextBlock Text="{DynamicResource Loc.Dashboard.RuleLabel}" 
                               Style="{StaticResource SectionHeaderStyle}" 
                               FontSize="28"
                               Foreground="{DynamicResource PrimaryTextBrush}"/>

                        <ItemsControl ItemsSource="{Binding Rules}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource RuleCardStyle}" Cursor="Hand">
                                        <Border.InputBindings>
                                            <MouseBinding Gesture="LeftClick" 
                  Command="{Binding Path=DataContext.OpenRuleCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}"
                  CommandParameter="{Binding}"/>
                                        </Border.InputBindings>

                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Grid.Row="0" Text="{Binding Title}"
                                                       Foreground="{DynamicResource PrimaryTextBrush}" 
                                                       FontWeight="Bold" 
                                                       TextAlignment="Center" TextWrapping="Wrap"
                                                       VerticalAlignment="Center" 
                                                       Margin="10" 
                                                       FontSize="16"
                                                       MaxWidth="200" MinWidth="200"/>

                                            <TextBlock Grid.Row="1" Text="{Binding Description}" 
                                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                                       FontWeight="Thin" 
                                                       TextAlignment="Center" 
                                                       TextWrapping="Wrap"
                                                       VerticalAlignment="Center" 
                                                       Margin="10" 
                                                       FontSize="{DynamicResource BaseFontSize}"
                                                       MaxWidth="200" MinWidth="200"/>

                                            <StackPanel Grid.Row="2" 
                                                        Orientation="Horizontal" 
                                                        HorizontalAlignment="Right" 
                                                        Margin="0,0,10,10"
                                                        Visibility="{Binding DataContext.CanManage, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}">

                                                <!-- Share Button -->
                                                <Button Content="📤"
                                                        Command="{Binding DataContext.ShareRuleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"                                                    
                                                        Foreground="{DynamicResource PrimaryAccentBrush}"
                                                        Background="Transparent" 
                                                        BorderThickness="0" 
                                                        Cursor="Hand"
                                                        FontSize="16" 
                                                        Margin="0,0,5,0" 
                                                        ToolTip="Share Rule"/>

                                                <Button Content="✏️"
                                                        Command="{Binding DataContext.EditRuleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"                                                    
                                                        Foreground="{DynamicResource PrimaryTextBrush}"
                                                        Background="Transparent" 
                                                        BorderThickness="0" 
                                                        Cursor="Hand"
                                                        FontSize="16" 
                                                        Margin="0,0,5,0" 
                                                        ToolTip="Edit Rule"/>

                                                <Button Content="🗑️"
                                                        Command="{Binding DataContext.DeleteRuleCommand, 
                                                                 RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        Background="Transparent" 
                                                        BorderThickness="0" 
                                                        Cursor="Hand"
                                                        Foreground="{DynamicResource DangerBrush}" 
                                                        FontSize="16" 
                                                        ToolTip="Delete"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Separator Margin="20"/>

                        <TextBlock Text="{DynamicResource Loc.Dashboard.VocabLabel}" 
                               Style="{StaticResource SectionHeaderStyle}" 
                               FontSize="28"
                               Foreground="{DynamicResource PrimaryTextBrush}"/>

                        <!-- Starter Pack (§18.2d LEARNING_IMPROVEMENTS) -->
                        <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="16" Padding="24,20" Margin="0,0,0,16"
                                BorderBrush="{DynamicResource PrimaryAccentBrush}" BorderThickness="2"
                                Visibility="{Binding ShowStarterPack, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="🎒 Начните обучение!" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                    <TextBlock Text="Установите стартовый набор слов, чтобы начать тренировку прямо сейчас." FontSize="14"
                                               Foreground="{DynamicResource SecondaryTextBrush}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>
                                <Button Grid.Column="1" Command="{Binding InstallStarterPackCommand}"
                                        Style="{StaticResource NavButtonStyle}" Padding="20,12" Margin="16,0,0,0"
                                        VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🚀" FontSize="18" VerticalAlignment="Center"/>
                                        <TextBlock Text="Установить" Margin="8,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="15"/>
                                    </StackPanel>
                                </Button>
                            </Grid>
                        </Border>

                        <ItemsControl ItemsSource="{Binding DisplayDictionaries}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource VocabCardStyle}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <Grid Grid.Row="0" Margin="20,15,15,5">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0" Text="{Binding Name}" 
                                                           FontSize="{DynamicResource HeaderFontSize}" 
                                                           Foreground="{DynamicResource PrimaryTextBrush}"
                                                           FontWeight="Bold" 
                                                           TextWrapping="Wrap" 
                                                           MaxHeight="40" 
                                                           TextTrimming="CharacterEllipsis">
                                                    <TextBlock.ToolTip>
                                                        <ToolTip Content="{Binding Name}"/>
                                                    </TextBlock.ToolTip>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#1F2937"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsFeatured}" Value="True">
                                                                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <Button Grid.Column="1"
                                                    Command="{Binding DataContext.ManageDictionaryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Width="30" Height="30"
                                                    Background="Transparent" BorderThickness="0" Cursor="Hand"
                                                    ToolTip="{DynamicResource Loc.Dashboard.DictionaryToolTip}">
                                                    <Button.Content>
                                                        <TextBlock Text="⚙️" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="16"/>
                                                    </Button.Content>
                                                </Button>
                                            </Grid>

                                            <StackPanel Grid.Row="1" Margin="20,0,20,0">
                                                <TextBlock Text="{Binding Description}" 
                                                            FontSize="14" Opacity="0.8" Margin="0,5,0,5" 
                                                            TextWrapping="Wrap" MaxHeight="40" TextTrimming="CharacterEllipsis">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsFeatured}" Value="True">
                                                                    <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <TextBlock FontSize="12" Opacity="0.6">
                                                    <Run Text="{DynamicResource Loc.Dashboard.WordCount}" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                                    <Run Text="{Binding WordCount, Mode=OneWay}" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold"/>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#1F2937"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsFeatured}" Value="True">
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </StackPanel>

                                            <Button Grid.Row="2" 
                                                Style="{StaticResource PlayButtonStyle}"
                                                Background="Transparent"
                                                Command="{Binding DataContext.StartLearningCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Bottom"
                                                Margin="15"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
